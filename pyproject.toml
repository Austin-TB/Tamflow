[project]
name = "tldrbot"
version = "0.1.0"
description = "Telegram summarization bot"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "langgraph>=1.1.8",
    "python-dotenv>=1.2.2",
    "python-telegram-bot>=22.7",
]
